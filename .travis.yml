language: java
sudo: false

cache:
  directories:
  - "$HOME/.m2"

jobs:
  include:
  - stage: test
    jdk: oraclejdk8
    script: mvn -q verify
  - jdk: oraclejdk8
    env: JAVADOC=true
    script:
    - mvn clean verify -DskipTests=true
    - mvn javadoc:aggregate

branches:
  only:
  - master
